import { View, Text } from "react-native";

export default function Settings() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text>Settings. You have to have a settings screen, right?</Text>
    </View>
  );
}
